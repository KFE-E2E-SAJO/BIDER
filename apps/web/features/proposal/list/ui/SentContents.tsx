import { ProposalContentsProps } from '@/features/proposal/list/types';

const SentContents = ({ filter }: ProposalContentsProps) => {
  return <div>SentContents</div>;
};

export default SentContents;
